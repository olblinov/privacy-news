{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Serve App",
            "type": "shell",
            "command": "npx quartz build --serve",
            "options": {
                "env": {
                    "PATH": "/Users/ol.blinov/.nvm/versions/node/v23.5.0/bin:${env:PATH}"
                }
            },
            "problemMatcher": []
        },
        {
            "label": "Push to GitHub Pages",
            "type": "shell",
            "command": "npx quartz sync && open 'https://github.com/olblinov/privacy-news/actions'",
            "options": {
                "env": {
                    "PATH": "/Users/ol.blinov/.nvm/versions/node/v23.5.0/bin:${env:PATH}"
                }
            }
        },
        {
            "label": "Test",
            "type": "shell",
            "command": "node --version",
            "options": {
                "env": {
                    "PATH": "/Users/ol.blinov/.nvm/versions/node/v23.5.0/bin:${env:PATH}"
                },
                "statusbar": {
                    "hide" : true
                }
            }
        }
    ]
}